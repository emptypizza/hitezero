[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA: 실제 파일이 없는 경로만 index.html로 (정적 에셋은 그대로 제공)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
