# HiteZero — 인계 (2026-06-15) · 터미널 에이전트 → Claude Cowork

대상: **Claude Cowork**(샌드박스 — 로컬 Godot/Android SDK 없음, 빌드·서명·git commit 불가). 직전 작업자: 터미널 에이전트(로컬 빌드·서명·커밋·CI·에뮬 검증 수행).
브랜치 `neon-style-7cc31` (origin과 sync) · GitHub `emptypizza/hitezero` · Godot **4.6.2** GL Compatibility · 패키지 `com.gghf.hitezero` · versionCode 1 / 1.0.0.

---

## 0. 한 줄 상황
**빌드·서명·CI·실기(에뮬) 검증까지 전부 끝났다. 기술적으로 출시 가능.** 남은 1순위는 순수하게 **사람 계정 작업**(Play 개발자 등록 $25 · Console 업로드 · 제출) + **웹 라이브 배포 계정 전환**뿐. Cowork가 도울 영역 = 제출 칸별 안내 / 스토어 카피·스크린샷 폴리시 / 웹배포 조율 / 포스트런치.

---

## 1. 지금 상태 (사실)

### 빌드 산출물 — ⚠️ 위치 주의
- **서명 AAB (Play 업로드용)**: `~/hitezero_release/hitezero-android/hitezero.aab` (28.7MB) — 업로드 키 cert `8F:21:9B:30:0C:B8:1F:74:06:31:A7:F2:60:7C:2D:6E:81:84:7E:86:6A:71:27:C5:88:24:38:44:C8:42:A1:62` 서명 확인.
- **서명 APK (사이드로드/테스트)**: 같은 폴더 `hitezero.apk` (75MB).
- 🔴 **레포 `build/`·`dist/`는 쓰지 말 것** — 이 머신은 레포 폴더의 **untracked 대용량 파일이 세션 중 증발**하는 싱크/마운트 이슈가 있다(핸드오프 누차 경고; 실제로 `build/`·`dist/`·`assets/kk.zip` 증발 발생). 그래서 산출물을 `$HOME`(증발 안 함, 키스토어처럼)에 둔 것. **재취득은 CI 아티팩트에서**: `gh run download 27501223945 -R emptypizza/hitezero -D ~/hitezero_release`.

### 서명 키
- 업로드 키: `~/keystores/hitezero_upload.jks` + 비번 `~/keystores/hitezero_upload.password.txt` (별칭 `upload`). **첫 출시 시 Play App Signing이 이 키를 영구 업로드키로 고정 → 반드시 오프라인 백업.**
- GitHub Secrets 등록 완료: `ANDROID_KEYSTORE_BASE64` / `ANDROID_KEY_ALIAS` / `ANDROID_KEYSTORE_PASSWORD` (이 키스토어 기준, cert 일치 확인됨).

### CI (작동 확인됨)
- `.github/workflows/android.yml` — `git tag v* && git push`(또는 Actions 수동 실행) → **서명 AAB+APK 자동 빌드** → 아티팩트 `hitezero-android`. rc3 run `27501223945` 성공(2m54s)으로 검증.
- 해결한 함정 2가지(코드에 박힘): ① `godot --install-android-build-template` 무한대기 → `android_source.zip` 직접추출 + `.gdignore`(재귀 패킹 차단). ② 헤드리스 export의 SDK/Java 경로 누락 → export 직전 editor settings 주입.

### 게임 (grupo B) — 실기 검증 완료
- 에뮬레이터(Pixel_3a API34 arm64) 설치·실행 → 타이틀+인게임 렌더링, **CEL 2.5D 슬랩 깊이 · 소프트 일시정지(II) · x2 · 저글 HUD 동작, 셰이더/스크립트 에러·크래시 0**.
- grupo B 내용: CEL 2.5D 비주얼·젖은바닥 반사·버블 VFX, 절차적 애니 리그, 소프트 일시정지(NEW-01), 트레이 저글 보너스(FL-02), 후반 BRUTAL 패턴(LD-01), 음소거 영구저장, 성능 최적화. 동봉 테스트 5종 전부 PASS.

### Play 제출 문서 (마감됨, submission-ready)
- `godot/docs/play_console_submission.md` — 7에이전트 정책 감사 + 캐릭터 아트 시각검수로 확정:
  - 기본 언어 **영어(en-US)** + 한국어(ko-KR) 추가 · 카테고리 **아케이드** · 타깃 **13+**(만13세미만 미포함=가족정책 비대상)
  - IARC: 판타지/만화 폭력=**예**, 나머지=아니요. **성적콘텐츠=아니요**(메이드=치비 전신의상, 노출 없음 — 실제 텍스처·스샷 검수로 방어 가능 확정)
  - 데이터 안전=**수집 안 함/공유 안 함**(인터넷 권한 없음·완전 오프라인, 로컬 save.cfg만) · 광고/IAP=없음 · 미국 수출법=동의
  - 서명 섹션 교정: 업로드키 `hitezero_upload.jks`, cert SHA-256 기록, CI 빌드 경로 명시.

### 웹
- grupo B 웹 번들은 빌드됨(`dist/godot-web/site_nothreads/` — 단 gremlin으로 증발 가능, `bash godot/tools/build_web.sh dist/godot-web`로 재생성, `godot`가 PATH에 필요).
- **라이브 `https://hitezero.netlify.app`는 아직 grupo B 이전(6/13) 빌드.** 업데이트하려면 재배포 필요.
- 개인정보처리방침 라이브: `https://hitezero.netlify.app/privacy` (소스 `store_assets/privacy.html`) — 유지.

---

## 2. 이번 세션 변경분 (전부 커밋·푸시 완료)
```
b54aa95 ci(android): editor settings(SDK/Java 경로) 주입
01d0d36 ci(android): install-android-build-template 무한대기 → 수동추출 우회
a88e6a1 chore: godot/android/ (생성 빌드템플릿) gitignore
b4bed77 docs(play): 제출 답안 마감 + 업로드키명 교정
263ca71 ci: stale verify-netlify-artifacts.yml 제거 (neon)
d4ecb97 docs: 핸드오프·워크리스트·디자인노트·progress
f9761c0 chore(release): Android export·인프라·아이콘·store_assets + 옛 웹번들 삭제
9458330 feat(godot): CEL 2.5D·애니리그·소프트 일시정지·트레이 저글 (grupo B)
```
+ origin/main에서도 `verify-netlify-artifacts.yml` 제거(GitHub API 커밋 `d15eb50`). 검증 태그 rc1/rc2 정리, `v1.0.0-rc3`만 남김(성공 빌드).

---

## 3. 다음 작업 (1순위) — 누가 하나
- **[사용자만]** Play 개발자 계정($25)·신원확인 → Console에서 앱 생성(이름 HiteZero·기본 en-US·게임·무료) → **내부 테스트** 트랙 → `~/hitezero_release/hitezero-android/hitezero.aab` 업로드 → **Play App Signing 켜기** → `play_console_submission.md` 답안 복붙 제출. **키스토어 백업.**
- **[사용자 권장]** 실기 1대 풀플레이(에뮬 검증은 끝났으나 실기 1회 권장).
- **[Cowork 가능]** Play 제출 진행 중 칸별 안내, 스토어 스크린샷 2장 추가 큐레이션(현재 2장, 4장 권장), 스토어 카피 다듬기.
- **[Cowork or 사용자]** **웹 라이브 배포** — 로컬 netlify CLI는 `starofdark@gmail.com`으로 로그인돼 있어 `hitezero` 사이트 **접근 불가**. 해결: (a) `hitezero` 소유 계정(team.gghf 추정)으로 `netlify login` 후 터미널 배포, 또는 (b) `dist/godot-web/site_nothreads/`를 app.netlify.com/drop에 드래그, 또는 (c) **Cowork가 원래 Netlify 배포를 했다면 Cowork 측에서 grupo B 번들로 재배포**.

---

## 4. PARKED (부르기 전엔 손대지 말 것)
- Higgsfield 추가 아트 통합 — **셀룩 자체는 이미 grupo B로 게임에 반영됨**. 추가 캐릭터/배경 셀 통합은 별도 판단(출시 후).
- 다른 프로젝트(Nemo_DO 등)는 범위 밖.

---

## 5. 룰 (하드)
- 🔴 **레포 `build/`·`dist/` 등 untracked 대용량은 세션 중 증발한다** — 산출물은 `$HOME` 또는 CI 아티팩트에서. "있다"고 가정 말고 실재 확인 후 사용.
- 시크릿 커밋 금지(`*.jks`·비번 — `.gitignore`로 막음, GitHub Secrets 사용).
- 게임플레이 계약 유지(`godot/docs/gameplay_spec.md`). grupo B 신규 룰(일시정지/저글/후반난이도)은 spec 갱신 권장.
- git 커밋은 사람 확인 후: 변경 파일 목록+요약 보고 먼저.
- Play엔 AAB만(APK는 테스트용). versionCode는 업데이트마다 +1.

---

## 6. 참고 (경로·명령)
- 업로드 AAB: `~/hitezero_release/hitezero-android/hitezero.aab`
- AAB 재취득: `gh run download 27501223945 -R emptypizza/hitezero -D ~/hitezero_release`
- 새 빌드: `git tag v1.0.0 && git push origin v1.0.0` → Actions 아티팩트
- 제출 답안: `godot/docs/play_console_submission.md`
- 출시 가이드: `godot/docs/google_play_release.md` / CI: `godot/docs/android_ci.md`
- 서명 키: `~/keystores/hitezero_upload.jks` (cert `8F:21:…:A1:62`, 별칭 `upload`)
